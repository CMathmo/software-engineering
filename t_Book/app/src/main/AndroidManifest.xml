<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wad.tBook">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".accounting.AccountingActivity"
            android:label="@string/title_activity_accounting"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".statistical.OtherStatisticalActivity" />
        <activity android:name=".statistical.AccountDetailActivity" />
        <activity
            android:name=".statistical.StatisticalActivity"
            android:label="@string/title_activity_statistical"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".accounting.AddActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>